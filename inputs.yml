# encoding: utf-8
#
# This is an example of an input file that can be passed to the profile
# to override the defaults of the inputs defined in each of the tests.
# You can use this to automatically adjust your tests when using automation or
# programmatic deployment or validation of different deployment patterns without
# having to adjust the core profile.

pg_owner: 'postgres'
pg_group: 'postgres'
pg_owner_password: 'postgres'

pg_dba: 'postgres'
pg_dba_password: 'postgres'
pg_db: 'db'

pg_user: 'user'
pg_host: 'localhost'
pg_port: '5432'

pg_version: '9.6'

pg_data_dir: "/var/lib/pgsql/9.5/data"
pg_conf_file: "/var/lib/pgsql/9.5/data/postgresql.conf"
pg_user_defined_conf: "/var/lib/pgsql/9.5/data/stig-postgresql.conf"
pg_hba_conf_file: "/var/lib/pgsql/9.5/data/pg_hba.conf"
pg_ident_conf_file: "/var/lib/pgsql/9.5/data/pg_ident.conf"

pg_shared_dirs: [
  "/usr/pgsql-9.5",
  "/usr/pgsql-9.5/bin",
  "/usr/pgsql-9.5/lib",
  "/usr/pgsql-9.5/include",
  "/usr/pgsql-9.5/share"
  ]

pgaudit_installation: ''

pg_superusers: [
  'postgres'
  ]

pg_users: [
  ''
  ]

pg_replicas: [
  '192.168.1.3/32',
  ]

pg_max_connections: '100'

pg_timezone: 'UTC'

approved_auth_methods: [
  'gss',
  'sspi',
  'ldap'
  ]

approved_packages: [
  ''
  ]

approved_ext: [
  ''
  ]

pg_object_granted_privileges: 'arwDxt'

pg_object_public_privileges: 'r'

pg_object_exceptions: [
  'pg_setting'
  ]
